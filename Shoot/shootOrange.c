#include"structs.h"

int maxAbs (int x, int y){
	if(x<0){
		x = -x;
	}
	if(y<0){
		y = -y;
	}
	return (maxAbs=(x<y)? x : y);
}

shoot* shootOrange(int dim){
	shoot* firstBlood=malloc(sizeof(shoot);
	firstBllod->dimension=dim;
	firstBllod->speed=8;//speed of shoot
	firstBllod->posX=250;//abscissa of the shoot
	firstBllod->posY=250;//ordinate of the shoot
	firstBllod->progressX=x-posX;//normal progress of the abscissa
	firstBllod->progressY=Y-posY;//normal progress of the ordinate
	firstBllod->maxXY=max(ProgressX,ProgressY);
	firstBllod->progressX =/ max;
	firstBllod->progressY =/ max;
}

